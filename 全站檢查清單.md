# R9 全外連修正版檢查

index.html: .mp4=0, .mp3=0, SunoCreate=0
  missing local src/href refs=4
    ${escapeHTML(clip.poster)}
    ${escapeHTML(file)}
    ${escapeHTML(work.img)}
    ${escapeHTML(work.thumb || work.img)}
music.html: .mp4=0, .mp3=0, SunoCreate=0
  missing local src/href refs=0

- 影片：不再依賴本機 MP4；未有獨立影片網址者導向 YouTube 頻道影片頁。
- 音樂：不再依賴本機 MP3；目前導向 Suno 首頁，取得每首公開歌曲網址後可逐首替換。
- 圖片：HTML 盡量改指向實際存在的 WebP。
